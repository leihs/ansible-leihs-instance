---
dependencies:
  - { role: leihs-user }
  - { role: phusion-passenger }
  - role: k0st1an.rbenv
    rbenv_user: "{{ user }}"
    rbenv_ruby_version: "{{ ruby_version }}"
    rbenv_ruby_initial: 'local'
    # rbenv:
    #   user: "{{ user }}"
    #   version: v0.4.0
    #   ruby_version: "{{ ruby_version }}"
