---
dependencies:
  - { role: leihs-user }
  - { role: phusion-passenger }
  - role: leonidas.rbenv
    rbenv:
      user: "{{ user }}"
      version: v0.4.0
      ruby_version: "{{ ruby_version }}"
#  - { role: phusion-passenger-vhost }
